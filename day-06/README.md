# DAY 6: Docker Networking

## 🎯 Cele:
- Sieć backend (DB + Cache)
- Sieć frontend (Services + Nginx)
- DNS resolution między serwisami
- Nginx routing

## 🚀 Jak odpalić:

```bash
cd day-06
docker-compose build
docker-compose up -d
bash scripts/test-networking.sh
